# 投影配图生成记录

生成方式：内置 image_gen；工具没有开放 GPT6 模型选择。四张新图按本地课件的数学内容编写提示词，不是原课件截图。

## 初始提示词

### 01-正交投影标准化

```text
Use case: scientific-educational. Create a precise, clean Chinese textbook diagram for a beginner learning GAMES101 projection. Landscape 3:2, high resolution. White background, dark navy typography, teal blue for main geometry, amber for highlighted quantities, restrained flat schematic style. Large perfectly legible Simplified Chinese, ample spacing, no decorative scenery, no watermarks, no extraneous text, no handwritten lettering. Diagrams must match the numeric labels and obey the geometry.
Title "正交投影：先居中，再缩放".
Three horizontal panels linked by arrows, each contains a horizontal number line with equally spaced ticks and one highlighted interval. All use the same drawing scale (40 px per mathematical unit), so widths visibly change correctly.
Panel 1 header "原范围 [2, 6]": ticks 0,2,4,6. Highlight endpoints 2 and 6 teal; middle 4 amber. Label "中心是 4，宽度是 4".
Arrow label "所有坐标减 4".
Panel 2 header "居中 [-2, 2]": ticks -2,0,2, highlight interval -2 to2 SAME LENGTH as first interval, middle0 amber. Label "中心变成 0，宽度仍是 4".
Arrow label "所有坐标乘 1/2".
Panel 3 header "标准范围 [-1, 1]": ticks -1,0,1, interval HALF LENGTH of previous intervals, middle0 amber. Label "中心是 0，宽度变成 2".
Below panels show three compact aligned rows:
"左边界：2 → -2 → -1"
"中间点：4 → 0 → 0"
"右边界：6 → 2 → 1"
Bottom emphasized text "x' = (x - 4) / 2".
Small footer "X、Y、Z 各自居中和缩放，长方体就变成标准立方体。"
Ensure first interval sits entirely to right of origin, last two straddle zero. This is NOT an arbitrary global zoom of each panel; preserve relative interval lengths.
```

### 02-透视相似三角形

```text
Use case: scientific-educational. Create a precise, clean Chinese textbook diagram for a beginner learning GAMES101 projection. Landscape 3:2, high resolution. White background, dark navy typography, teal blue for main geometry, amber for highlighted quantities, restrained flat schematic style. Large perfectly legible Simplified Chinese, ample spacing, no decorative scenery, no watermarks, no extraneous text, no handwritten lettering. Diagrams must match the numeric labels and obey the geometry.
Title "透视投影：近大远小来自相似三角形".
Two equally sized side-by-side panels. Use an EXACT mathematical side view in both, horizontal axis "距离 d = -z" grows right, vertical axis "高度 y" grows up. Same axis scales and view ranges d0..4.5,y0..2.4 in both. Eye is O at (0,0). Near image plane is vertical at d=1 drawn dashed teal and labeled "近平面 d=1". All vertical bars start on y=0. Use a thin projection ray from O to the top of object.
Left panel title "物体距离 2，高度 2". Object orange vertical segment at d=2 from y0 to2. Ray O to(2,2) intersects near plane at(1,1); show thick teal projected segment at d1 from y0 to1 and label "投影高度 1". Put tick2 at object depth. Below "2 × (1/2) = 1".
Right panel title "物体距离 4，高度 2". Object orange vertical segment at d4 from y0 to2 SAME DRAWN HEIGHT as left panel. Ray O to(4,2) intersects near plane at(1,0.5). Show teal projected segment d1 from y0 to0.5 HALF THE HEIGHT of the left projected segment. Label "投影高度 0.5". Below "2 × (1/4) = 0.5".
Footer "同样高的物体，距离加倍，投影高度减半。"
Second footer "图中用正距离 d；课件写作 z=-d，近平面坐标 n=-1。"
Do not make ray extend to different object position, no fake 3D perspective. Keep exact intersection ratios.
```

### 03-视锥体到标准立方体

```text
Use case: scientific-educational. Create a precise, clean Chinese textbook diagram for a beginner learning GAMES101 projection. Landscape 3:2, high resolution. White background, dark navy typography, teal blue for main geometry, amber for highlighted quantities, restrained flat schematic style. Large perfectly legible Simplified Chinese, ample spacing, no decorative scenery, no watermarks, no extraneous text, no handwritten lettering. Diagrams must match the numeric labels and obey the geometry.
Title "老师的两步法：先产生透视效果，再标准化".
Three clear separated horizontal panels with large action arrows. All shapes are explicitly 2D side cross-sections (not full 3D), horizontal axis is positive forward distance d; vertical is y.
Panel1 header "视锥体的截面". Show truncated triangular region with near vertical edge at d1 extending y=-1 to+1 and far vertical edge at d5 extending y=-5 to+5. Sloping top and bottom edges extend toward dotted eye O at d0. Four-sided trapezoid with narrow LEFT vertical edge and wide RIGHT vertical edge. Caption "近处窄，远处宽".
Arrow caption "透视挤压 Q".
Panel2 header "长方体的截面". Show rectangle d1..5 and y=-1..1. Do not draw eye or rays. Caption "远处横纵收缩更多" and second line "近远边界保留，内部深度会重映射".
Arrow caption "正交标准化 O".
Panel3 header "标准立方体的截面". Show square with horizontal coordinate axis explicitly "标准化深度", vertical axis y. All sides range -1..1, central origin0. Depth axis +1 on LEFT edge and -1 on RIGHT edge (so near at left maps+1 and far right maps-1). Vertical +1 top and -1 bottom. A caption "各轴范围统一为 [-1, 1]".
Bottom bold "近大远小在第一步形成；第二步不会把它取消。"
Small footer "示意图不按三幅图统一比例绘制；深度符号采用课件约定。"
Do not include incorrect camera after standardizing, do not claim every point depth unchanged, do not flatten all depth to near plane.
```

### 04-齐次坐标计算过程

```text
Use case: scientific-educational. Create a precise, clean Chinese textbook diagram for a beginner learning GAMES101 projection. Landscape 3:2, high resolution. White background, dark navy typography, teal blue for main geometry, amber for highlighted quantities, restrained flat schematic style. Large perfectly legible Simplified Chinese, ample spacing, no decorative scenery, no watermarks, no extraneous text, no handwritten lettering. Diagrams must match the numeric labels and obey the geometry.
Title "矩阵算完，还要除以 w".
Use a large legible FOUR step flow, two rows in a 2x2 serpentine layout with explicit numbered arrows 1→2→3→4 (top left to top right, then bottom left then bottom right); no extraneous cross-arrows.
Top subtitle "同一个点的完整计算：n=-1，f=-5，近平面左右上下边界均为 ±1".
Panel1 "① 相机坐标" vector written horizontally "(1, 1, -2, 1)". Small explanation "位置：右 1，上 1，前方 2".
Panel2 "② 乘透视挤压矩阵 Q" tuple "(-1, -1, 7, -2)". Highlight last component amber with label "w = 原来的 z = -2". Small explanation "这是齐次坐标，还不是三维坐标".
Panel3 "③ 乘正交矩阵 O" tuple "(-1, -1, 0.5, -2)". Explanation "此例 O 将深度居中并缩放".
Panel4 "④ 前三个分量除以 w" expression "(-1, -1, 0.5) ÷ (-2)" then large tuple "(0.5, 0.5, -0.25)". Header badge "NDC". Last note "左右、上下、深度均在 [-1, 1] 内".
Bottom bold "实际计算可以先合并 P = OQ，最后只做一次齐次除法。"
Footer "这是课件的符号约定；不要直接当作任意图形 API 的投影矩阵。"
Exact arithmetic, no extra matrix definitions, no conflicting decimals, no arrows suggesting divide each component by a different value.
```

## 视觉复核后的修订提示词

第一图修正刻度比例，第三图修正深度轴方向与标签，第四图移除会混淆齐次坐标和三维位置的装饰性空间图。

### 01-正交投影标准化

```text
Edit this existing educational diagram. Preserve ALL Chinese text, equations, colors, panel layout and all numeric values. Correct only the geometry of the three horizontal number lines so a unit has exactly the same drawn length in all panels. In panel 1, distance between endpoint2 and endpoint6 should be 200 pixels, center4 at the exact midpoint, origin0 100 pixels left of endpoint2. In panel2 endpoints-2 and2 also 200 pixels apart, center0 midpoint. In panel3 endpoints-1 and1 exactly100 pixels apart, center0 midpoint. First and second intervals MUST have the same drawn length; third interval MUST be exactly half that length. Move vertical dashed dimension guides and endpoint arrows to match these corrected positions. Labels remain legible with sufficient spacing. Do not change any mathematical content. No new decorations.
```

### 03-视锥体到标准立方体

```text
Edit this existing educational diagram. Preserve all text, three panels, shapes, colors, and layout except the following precise fixes. In third/rightmost panel, the horizontal standardized depth axis currently has +1 at left and -1 at right but arrow points right: reverse ONLY that horizontal axis arrow so it points LEFT toward increasing values. Keep +1 at left, -1 at right, and 0 in center. Move the label 标准化深度 slightly if necessary to keep it readable. In first and second panels replace subscript-style d_0 d_1 d_5 with explicit text d=0 d=1 d=5. For middle panel label horizontal axis d_s (since depth has been remapped), and use d_s=1 and d_s=5 for the two end labels. All other text including internal depth remapping caption remains unchanged. No new formulas or decoration.
```

### 04-齐次坐标计算过程

```text
Edit this educational figure to remove misleading 3D geometry. Keep title, subtitle, 4 panel headers, every numeric tuple and equation EXACTLY unchanged. REMOVE ALL four 3D illustrations and coordinate axes from every panel; they misleadingly suggest that pre-division tuples can be plotted as 3D positions. Replace the freed space with larger centered existing tuples and text, making this a clean arithmetic flow infographic. Preserve navy, teal and amber palette. Panel1 (1,1,-2,1) with caption 位置：右1，上1，前方2. Panel2 (-1,-1,7,-2), w=原来的z=-2, 这是齐次坐标，还不是三维坐标. Panel3 (-1,-1,0.5,-2), 此例O将深度居中并缩放. Panel4 (-1,-1,0.5)÷(-2), result(0.5,0.5,-0.25), badge NDC. Add unambiguous arrows connecting 1→2→3→4: top-left1 to top-right2, a connecting arrow from top-right2 into bottom-left3 routed in inter-panel whitespace, then bottom-left3 to bottom-right4. Do not add a 1→3 or2→4 shortcut. Footer remains 实际计算可以先合并 P=OQ，最后做一次齐次除法. Preserve the final small disclaimer. No axes, no cubes, no new mathematical content.
```

