## C# System.Object对象如何判空

1. **原理**：当xxx被销毁时，lua侧的userdata还在，但是C#侧的对象已经被销毁，此时使用xxx == nil无法判断出对象是否为空。解决方案如下（以DouyinActor为例）：
2. 需要先判断Lua对象是否为nil，同时判断 CS.System.Object.ReferenceEquals(actor, nil)

```lua
if actor == nil or CS.System.Object.ReferenceEquals(actor, nil) then
end
```

3. 哪些对象是System.Object
	- DouyinPlayer
	- DouyinPet
	- DouyinObject
	- DouyinActor
## UnityEngine.Object对象/UnityEngineGameObject对象如何判空
1. **原理**：当UnityEngine.Object/GameObject被销毁时，Lua侧的userdata还在，但是Unity以及C#侧的对象已经被销毁，此时使用xxx == nil无法判断出对象是否为空。解决方案如下
2. 需要先判断Lua对象是否为nil，可以使用 xxx.IsNull() 或者 xxx.Equals(nil) 来判断Unity Object是否为nil
3. 如果对象是GameObject，需要使用 IsNull() 进行判空
```lua
local function IsValidRef(obj)
    return obj ~= nil and not obj:IsNull()
end
```