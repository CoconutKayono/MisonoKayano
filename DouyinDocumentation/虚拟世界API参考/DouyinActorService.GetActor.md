public static DouyinActor GetActor(GameObject go)
# 参数
| 参数 | 参数描述 |
| --- | --- |
| go | 用于查找对应 Actor 的游戏对象。该对象是游戏场景中已存在的对象 |
# 返回
DouyinActor
若在字典中找到与传入的 GameObject 对应的 Actor，则返回该 Actor。
若未找到，返回 null。
# 描述
通过 ID 获取 Actor。
