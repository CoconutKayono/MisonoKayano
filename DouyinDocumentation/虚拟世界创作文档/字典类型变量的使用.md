目前SDK内对于字典（普通变量/同步变量）的使用方式可参考下方：

* ContainsKey(key)：判断key是否存在
* GetValue(key)：通过key获取value
* SetValue(key)：通过key设置value
* TryGetValue(key)：通过key获取value
* Remove(key)：通过key移除value
* Clean()：清除字典
* Dictionary.ForEach(function callback)：遍历字典


普通字典变量和同步变量在使用上是没有差别的，唯一的区别就是同步变量有 OnValueChange ，普通变量没有
