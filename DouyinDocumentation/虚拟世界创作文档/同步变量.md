同步变量是指会自动在玩家间同步数据的存储容器，例如一个数字、一个位置或者一段文字。
当同步变量所存储的数据变化时，房间内所有玩家都将会同步数据的变化，因为同步变量是一种用于处理数据同步相关的非常简单方便的方式。  
比如你想要实现一个房间内排行榜，所有人都可以看到相同的分数记录，就可以使用同步变量来存储玩家与他们的分数。
# 支持的变量类型

* 当前支持的同步变量类型包括：**bool, byte, short, int, long, float, double, string, Vector2, Vector3, Quaternion.**
* 也可声明同步的 **List** 和 **Dictionary**，Dictionary 的键类型允许是 **byte, short, int, long, string**，List 和 Dictionary 的值类型允许是以上的所有类型。

# 创建变量

* 使用** ---@varSync **开头，表示声明一个同步变量

```Lua
---@varSync intValue:int
---@varSync floatValue:float
---@varSync boolValue:bool
---@varSync vectorValue:Vector3
---@varSync listSync:List<int>
---@varSync dictionarySync:Dictionary<int,string>
---@end
```

<div style="text-align: center"><img src="https://p9-arcosite.byteimg.com/tos-cn-i-goo7wpa0wc/3fce7b9f10a945acb3182e9a66248fb0~tplv-goo7wpa0wc-image.image" width="264px" /></div>

这个 GameObject 的主权方将负责同步这些变量值，并且他们的更改将发送给其他人。
# 修改和使用变量
你可以使用 SetValue 和 GetValue 对变量赋值，和获取变量的值。  
请注意：

1. 同步变量无法同普通变量那样，直接用等号（=）给变量赋值。
2. 只有主权方可以修改同步变量，非主权方无法修改同步变量。

```Lua
function OnNetSpawned()
  local val = vectorValue:GetValue()
  print("current storage value",val)
  vectorValue:SetValue(UnityEngine.Vector3(1,2,0))
  intValue:SetValue(1)
  floatValue:SetValue(1.5)
  boolValue:SetValue(false)
end
```

# 当变量变化时
当变量被主权方改变后，房间内的其他所有玩家将会接收到这个变化。如果你需要在变量变化的时候做点什么，那么你可以通过变量具有的 **OnValueChange** 事件函数监听这一变化。  
请注意，**OnValueChange** 事件函数的注册，通常在 [DouyinScript.OnNetSpawned](https://vcreate.douyin.com/tutorial?book=4j78bceb&doc=tzydi4x1)<span style="color: #245bdb"> </span>事件函数中，因为此事件表示着网络对象都已经被初始化完成，可以进行后续的逻辑。如果注册的时间过早，网络对象还未生成，可能会运行错误。
关于同步变量的使用和**OnValueChange**时间函数的注册时机建议阅读 [世界生命周期和调用时序](/s196aspp/vxcl0pi3)详细了解时许内容。


```Lua
function OnNetSpawned()
    intValue:OnValueChange(function(curVal,preVal)
        print("intValue changed, current value = ", curVal, ", previous value = ", preVal)
    end)
  
  -- 如果需要使用之前的值，可以在OnNetSpawned方法里定义变量记录
    local preList = {}
    listSync:ForEach(function(value,idx)
        table.insert(preList,value)
    end)

    listSync:OnValueChange(function()        -- 注意：List 和 Dictionary 没有参数
        -- 使用listSync，读取当前值
        for i=0,listSync:Count() - 1 do
            print(string.format("current array index:%d %s", i, listSync:GetValue(i)))
        end
       -- 也可以使用ForEach来遍历
        listSync:ForEach(function(value,idx)
            print(string.format("current array index:%d %s", idx, value))
        end)
        
        -- 使用preList
        for i,value in ipairs(preList) do
            print(string.format("previous array index:%d %s", i, value))
        end

        -- 记得给preList赋值
        preList = {}
        listSync:ForEach(function(value,idx)
            table.insert(preList,value)
        end)
    end)
end
```

# 使用List
```Lua
---@varSync listSync:List<int>
---@end

function OnNetSpawned()
    local arrIdx = 0
    listSync:GetValue(arrIdx)
    listSync:SetValue(arrIdx,111)

    listSync:OnValueChange(function()
        for i=0,listSync:Count() - 1 do
            print(string.format("current array index:%d %s",i, listSync:GetValue(i)));
        end
        -- 也可以使用ForEach来遍历
        listSync:ForEach(function(value,idx)
            print(string.format("current array index:%d %s",idx, value));
        end)
    end)

    -- clear storage
    listSync:Clear()
    -- Append value 
    listSync:Add(200)
    -- insert element at index
    listSync:Insert(arrIdx,400)
    -- remove element
    listSync:RemoveAt(arrIdx)
end
```

# 使用Dictionary
```Lua
---@varSync dictionarySync:Dictionary<int,string>
---@end

function OnNetSpawned()
    local dicKey = 100
    -- add or change dictionary value
    dictionarySync:SetValue(dicKey,"dic value")

    if dictionarySync:ContainsKey(dicKey) then
        print("check dictionary has key",dictionarySync:GetValue(dicKey))
    end

    -- remove dictionary item
    dictionarySync:Remove(dicKey)
    -- clear storage
    dictionarySync:Clear()
    -- listen dictionary value change
    dictionarySync:OnValueChange(function()
        -- iterate the dictionary
    dictionarySync:ForEach(function(value,key)
            print(string.format("dictionarySync value changed key:%s value:%s",key,value));
        end)
    end)
    
    local dictionaryEleCount = dictionarySync:Count()
    print("dictionarySync contain element",dictionaryEleCount)
end
```


